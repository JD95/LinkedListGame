using UnityEngine;
using System.Collections;

public class NodeManager : MonoBehaviour {
	public BoxNode currentNode = null;
	public bool activeSelection = false; //basically if the player is controlling the arrow rn.
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
